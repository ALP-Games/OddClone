[gd_scene format=3 uid="uid://b2cxwvtwbjk0y"]

[ext_resource type="Script" uid="uid://d4ax3xntynw2k" path="res://actors/npc.gd" id="1_hshcy"]
[ext_resource type="Texture2D" uid="uid://bo64ihlpkpcjo" path="res://Assets/npc/KAREN-NORMAL.png" id="2_hshcy"]
[ext_resource type="Script" uid="uid://bjx3cmprqfi4x" path="res://scripts/sprite_with_effect.gd" id="3_sn3e3"]
[ext_resource type="Texture2D" uid="uid://d3anfdo05k4ly" path="res://Assets/npc/karenmask.png" id="4_hshcy"]
[ext_resource type="Texture2D" uid="uid://daad6jsa5rds7" path="res://Assets/npc/karenbody-NORMAL.png" id="5_hshcy"]

[sub_resource type="CylinderShape3D" id="CylinderShape3D_qbes0"]
radius = 0.3

[sub_resource type="Shader" id="Shader_0b5f4"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float vframes = 1.0;
uniform float y_offset = 0.0;
uniform sampler2D albedo_texture : source_color;

uniform float dissolve : hint_range(0, 1) = 1.0;
uniform float block_size = 16.0;
uniform float randomness : hint_range(0, 1) = 0.5;
uniform vec3 glow_color: source_color = vec3(1.0, 0.7, 0.3);
uniform float flicker_intensity : hint_range(0, 1) = 0.5;


void vertex() {
	vec3 local_up = MODEL_MATRIX[1].xyz;
	//cross of:
	//	local_transform.basis.y     and    to_world(view_space.forward)
	//normalized
	//(represents right direction)
	vec4 ax = vec4(normalize(cross(local_up, INV_VIEW_MATRIX[2].xyz)), 0.0);
	//local_transform.basis.y
	//(represents up direction)
	vec4 ay = vec4(local_up.xyz, 0.0);
	//cross of:
	//	to_world(view_space.right)    and    local_transform.basis.y
	//(represents forward direction)
	vec4 az = vec4(normalize(cross(INV_VIEW_MATRIX[0].xyz, local_up)), 0.0);
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(ax, ay, az, MODEL_MATRIX[3]);
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}


void fragment() {
	vec4 tex = texture(albedo_texture, UV);
	
	// Calculate block coordinates
	vec2 block_uv = floor(UV * block_size) / block_size;
	
	// Add random offset per block
	float block_seed = fract(sin(dot(block_uv, vec2(12.9898, 78.233))) * 43758.5453);
	float random_offset = block_seed * randomness;
	
	// Calculate dissolve position
	float pos = (UV.y * vframes) - y_offset;
	
	// Block-based dissolve
	float block_dissolve = dissolve * 1.7;
	float block_threshold = pos + random_offset;
	
	// Calculate flicker based on block position and dissolve progress
    // Creates a pattern without if statements
    float flicker_pattern = fract(block_uv.x * 7.3 + block_uv.y * 13.7) * 2.0 - 1.0;
    flicker_pattern = abs(flicker_pattern); // 0-1 range
    
    // Calculate if block is in dissolving zone (0 = solid, 1 = dissolved)
    float dissolve_zone = smoothstep(block_dissolve - 0.2, block_dissolve, block_threshold);
    
    // Calculate if block is in flicker zone (edge area)
    float flicker_zone = smoothstep(block_dissolve - 0.15, block_dissolve - 0.05, block_threshold);
    flicker_zone *= (1.0 - smoothstep(block_dissolve - 0.05, block_dissolve, block_threshold));
    
    // Flicker intensity based on block seed
    float flicker_strength = flicker_pattern * flicker_intensity * flicker_zone;
    
    // Combine effects
    float visibility = 1.0 - dissolve_zone; // 1 = visible, 0 = invisible
    
    // Apply flicker to visibility
    visibility += flicker_strength;
    visibility = clamp(visibility, 0.0, 1.0);
    
    // Calculate glow
    float glow = flicker_zone * flicker_strength;
    
    // Final color
    vec3 final_color = mix(tex.rgb, glow_color, glow * 0.7);
    final_color += glow_color * glow * 2.0; // Additive glow
    
    ALBEDO = final_color;
    ALPHA = tex.a * visibility;
    EMISSION = glow_color * glow * 3.0;
	
	//ALPHA_SCISSOR_THRESHOLD = 0.0001;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_3s6tw"]
render_priority = 2
shader = SubResource("Shader_0b5f4")
shader_parameter/vframes = 1.0
shader_parameter/y_offset = 0.0
shader_parameter/albedo_texture = ExtResource("2_hshcy")
shader_parameter/dissolve = 1.0
shader_parameter/block_size = 16.0
shader_parameter/randomness = 0.5
shader_parameter/glow_color = Color(0.6392157, 0.8235294, 0.6156863, 1)
shader_parameter/flicker_intensity = 0.5

[sub_resource type="Shader" id="Shader_o86md"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float vframes = 1.0;
uniform float y_offset = 0.0;
uniform sampler2D albedo_texture : source_color;

uniform float dissolve : hint_range(0, 1) = 1.0;
uniform float block_size = 16.0;
uniform float randomness : hint_range(0, 1) = 0.5;
uniform vec3 glow_color: source_color = vec3(1.0, 0.7, 0.3);
uniform float flicker_intensity : hint_range(0, 1) = 0.5;


void vertex() {
	vec3 local_up = MODEL_MATRIX[1].xyz;
	//cross of:
	//	local_transform.basis.y     and    to_world(view_space.forward)
	//normalized
	//(represents right direction)
	vec4 ax = vec4(normalize(cross(local_up, INV_VIEW_MATRIX[2].xyz)), 0.0);
	//local_transform.basis.y
	//(represents up direction)
	vec4 ay = vec4(local_up.xyz, 0.0);
	//cross of:
	//	to_world(view_space.right)    and    local_transform.basis.y
	//(represents forward direction)
	vec4 az = vec4(normalize(cross(INV_VIEW_MATRIX[0].xyz, local_up)), 0.0);
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(ax, ay, az, MODEL_MATRIX[3]);
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}


void fragment() {
	vec4 tex = texture(albedo_texture, UV);
	
	// Calculate block coordinates
	vec2 block_uv = floor(UV * block_size) / block_size;
	
	// Add random offset per block
	float block_seed = fract(sin(dot(block_uv, vec2(12.9898, 78.233))) * 43758.5453);
	float random_offset = block_seed * randomness;
	
	// Calculate dissolve position
	float pos = (UV.y * vframes) - y_offset;
	
	// Block-based dissolve
	float block_dissolve = dissolve * 1.7;
	float block_threshold = pos + random_offset;
	
	// Calculate flicker based on block position and dissolve progress
    // Creates a pattern without if statements
    float flicker_pattern = fract(block_uv.x * 7.3 + block_uv.y * 13.7) * 2.0 - 1.0;
    flicker_pattern = abs(flicker_pattern); // 0-1 range
    
    // Calculate if block is in dissolving zone (0 = solid, 1 = dissolved)
    float dissolve_zone = smoothstep(block_dissolve - 0.2, block_dissolve, block_threshold);
    
    // Calculate if block is in flicker zone (edge area)
    float flicker_zone = smoothstep(block_dissolve - 0.15, block_dissolve - 0.05, block_threshold);
    flicker_zone *= (1.0 - smoothstep(block_dissolve - 0.05, block_dissolve, block_threshold));
    
    // Flicker intensity based on block seed
    float flicker_strength = flicker_pattern * flicker_intensity * flicker_zone;
    
    // Combine effects
    float visibility = 1.0 - dissolve_zone; // 1 = visible, 0 = invisible
    
    // Apply flicker to visibility
    visibility += flicker_strength;
    visibility = clamp(visibility, 0.0, 1.0);
    
    // Calculate glow
    float glow = flicker_zone * flicker_strength;
    
    // Final color
    vec3 final_color = mix(tex.rgb, glow_color, glow * 0.7);
    final_color += glow_color * glow * 2.0; // Additive glow
    
    ALBEDO = final_color;
    ALPHA = tex.a * visibility;
    EMISSION = glow_color * glow * 3.0;
	
	//ALPHA_SCISSOR_THRESHOLD = 0.0001;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_iv687"]
render_priority = 1
shader = SubResource("Shader_o86md")
shader_parameter/vframes = 1.0
shader_parameter/y_offset = 0.0
shader_parameter/albedo_texture = ExtResource("4_hshcy")
shader_parameter/dissolve = 1.0
shader_parameter/block_size = 16.0
shader_parameter/randomness = 0.5
shader_parameter/glow_color = Color(0.6392157, 0.8235294, 0.6156863, 1)
shader_parameter/flicker_intensity = 0.5

[sub_resource type="Shader" id="Shader_3s6tw"]
code = "shader_type spatial;
render_mode unshaded, cull_disabled;

uniform float vframes = 1.0;
uniform float y_offset = 0.0;
uniform sampler2D albedo_texture : source_color;

uniform float dissolve : hint_range(0, 1) = 1.0;
uniform float block_size = 16.0;
uniform float randomness : hint_range(0, 1) = 0.5;
uniform vec3 glow_color: source_color = vec3(1.0, 0.7, 0.3);
uniform float flicker_intensity : hint_range(0, 1) = 0.5;


void vertex() {
	vec3 local_up = MODEL_MATRIX[1].xyz;
	//cross of:
	//	local_transform.basis.y     and    to_world(view_space.forward)
	//normalized
	//(represents right direction)
	vec4 ax = vec4(normalize(cross(local_up, INV_VIEW_MATRIX[2].xyz)), 0.0);
	//local_transform.basis.y
	//(represents up direction)
	vec4 ay = vec4(local_up.xyz, 0.0);
	//cross of:
	//	to_world(view_space.right)    and    local_transform.basis.y
	//(represents forward direction)
	vec4 az = vec4(normalize(cross(INV_VIEW_MATRIX[0].xyz, local_up)), 0.0);
	MODELVIEW_MATRIX = VIEW_MATRIX * mat4(ax, ay, az, MODEL_MATRIX[3]);
	MODELVIEW_NORMAL_MATRIX = mat3(MODELVIEW_MATRIX);
}


void fragment() {
	vec4 tex = texture(albedo_texture, UV);
	
	// Calculate block coordinates
	vec2 block_uv = floor(UV * block_size) / block_size;
	
	// Add random offset per block
	float block_seed = fract(sin(dot(block_uv, vec2(12.9898, 78.233))) * 43758.5453);
	float random_offset = block_seed * randomness;
	
	// Calculate dissolve position
	float pos = (UV.y * vframes) - y_offset;
	
	// Block-based dissolve
	float block_dissolve = dissolve * 1.7;
	float block_threshold = pos + random_offset;
	
	// Calculate flicker based on block position and dissolve progress
    // Creates a pattern without if statements
    float flicker_pattern = fract(block_uv.x * 7.3 + block_uv.y * 13.7) * 2.0 - 1.0;
    flicker_pattern = abs(flicker_pattern); // 0-1 range
    
    // Calculate if block is in dissolving zone (0 = solid, 1 = dissolved)
    float dissolve_zone = smoothstep(block_dissolve - 0.2, block_dissolve, block_threshold);
    
    // Calculate if block is in flicker zone (edge area)
    float flicker_zone = smoothstep(block_dissolve - 0.15, block_dissolve - 0.05, block_threshold);
    flicker_zone *= (1.0 - smoothstep(block_dissolve - 0.05, block_dissolve, block_threshold));
    
    // Flicker intensity based on block seed
    float flicker_strength = flicker_pattern * flicker_intensity * flicker_zone;
    
    // Combine effects
    float visibility = 1.0 - dissolve_zone; // 1 = visible, 0 = invisible
    
    // Apply flicker to visibility
    visibility += flicker_strength;
    visibility = clamp(visibility, 0.0, 1.0);
    
    // Calculate glow
    float glow = flicker_zone * flicker_strength;
    
    // Final color
    vec3 final_color = mix(tex.rgb, glow_color, glow * 0.7);
    final_color += glow_color * glow * 2.0; // Additive glow
    
    ALBEDO = final_color;
    ALPHA = tex.a * visibility;
    EMISSION = glow_color * glow * 3.0;
	
	ALPHA_SCISSOR_THRESHOLD = 0.0001;
}
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_sjbeq"]
render_priority = 0
shader = SubResource("Shader_3s6tw")
shader_parameter/vframes = 1.0
shader_parameter/y_offset = 0.0
shader_parameter/albedo_texture = ExtResource("5_hshcy")
shader_parameter/dissolve = 1.0
shader_parameter/block_size = 100.0
shader_parameter/randomness = 0.5
shader_parameter/glow_color = Color(0.6392157, 0.8235294, 0.6156863, 1)
shader_parameter/flicker_intensity = 0.5

[node name="NPC" type="CharacterBody3D" unique_id=778123170 node_paths=PackedStringArray("face", "head", "body") groups=["NPC"]]
collision_layer = 3
script = ExtResource("1_hshcy")
face = NodePath("Face")
head = NodePath("Head")
body = NodePath("Body")
animation_cycle_speed = 1.0
bad_animation_frame = 4

[node name="CollisionShape3D" type="CollisionShape3D" parent="." unique_id=696306462]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1, 0)
shape = SubResource("CylinderShape3D_qbes0")
debug_color = Color(0, 0.6, 0.69803923, 1)

[node name="Face" type="Sprite3D" parent="." unique_id=1575915531]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.7961043, 0)
material_override = SubResource("ShaderMaterial_3s6tw")
offset = Vector2(-34.76, -208.22)
pixel_size = 0.00055
billboard = 2
render_priority = 1
texture = ExtResource("2_hshcy")
hframes = 4
vframes = 4
script = ExtResource("3_sn3e3")
animation_frame = 0
animation_vframes = 4
metadata/_custom_type_script = "uid://bjx3cmprqfi4x"

[node name="Head" type="Sprite3D" parent="." unique_id=335742942]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 1.8312495, 0)
material_override = SubResource("ShaderMaterial_iv687")
offset = Vector2(-49.19, -123.25)
pixel_size = 0.0006
billboard = 2
texture = ExtResource("4_hshcy")
script = ExtResource("3_sn3e3")
animation_frame = 0
animation_vframes = 1
metadata/_custom_type_script = "uid://bjx3cmprqfi4x"

[node name="Body" type="Sprite3D" parent="." unique_id=2099446628]
transform = Transform3D(1, 0, 0, 0, 1, 0, 0, 0, 1, 0, 0.828, 0)
material_override = SubResource("ShaderMaterial_sjbeq")
offset = Vector2(50.62, 41.69)
pixel_size = 0.0025
billboard = 2
alpha_cut = 1
texture = ExtResource("5_hshcy")
hframes = 4
vframes = 2
script = ExtResource("3_sn3e3")
animation_frame = 0
animation_vframes = 2

[node name="BodyFrameCycle" type="Timer" parent="." unique_id=1510843439]

[node name="DisintegrateTimer" type="Timer" parent="." unique_id=975446416]
